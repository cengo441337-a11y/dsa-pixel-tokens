{
  "name": "dsa-pixel-tokens",
  "version": "0.8.0",
  "description": "Testgerüst für das FoundryVTT-Modul. Foundry selbst liest module.json, nicht diese Datei — hier steht ausschliesslich, wie die Regeltests laufen.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs",
    "mutation": "node tests/mutationspruefung.mjs",
    "check": "node tests/syntaxpruefung.mjs && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
